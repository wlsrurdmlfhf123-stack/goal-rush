import { BALL_ID, type MapDef } from "../maps/types";
import { GOAL_HALF_W, LANE_HALF, makeCourse } from "../maps/course";

/**
 * STAGE 2 — 양쪽 레버
 *
 * 이 스테이지의 규칙 하나: **문은 누가 눌러 두는 동안에만 열려 있다.**
 * 그래서 둘의 역할이 저절로 갈린다 — 한 명은 버티고, 다른 한 명이 공을 몰고
 * 문을 지난다. 그리고 지난 쪽이 다음 발판으로 뛰어가야 처음 사람이 따라온다.
 *
 * [세 가지 다른 「눌러 두기」를 만난다]
 *   -20  버튼 문      발판과 문이 한 몸. "밟으면 열린다"를 손해 없이 배운다
 *   -46  떨어진 레버  레버가 문에서 **14m 옆·뒤**에 있다. 밟은 쪽은 문을 못 본다
 *   -92  버튼 문      스위퍼 뒤. 문을 지난 쪽이 곧바로 다음 압박을 만난다
 * 같은 장치를 세 번 반복하는 게 아니라, **관계가 매번 다르다.**
 *
 * [-46 레버가 이 스테이지의 핵심이다]
 * 버튼 문은 발판이 문 바로 앞(3.6m)이라 밟은 사람이 문을 보면서 누른다.
 * 여기서는 레버가 코스 왼쪽 끝(x=-5.6)에 있고 문은 8m 앞이다. 밟은 쪽은
 * "지금 지나갔나"를 볼 수 없어서 **말로 물어봐야 한다.** 그 대사가 나오는
 * 것이 이 스테이지의 목표다.
 * 문은 `coopgate`(link) — 신호가 **하나라도** 켜지면 열린다. 레버가 하나뿐인
 * 이 배치에서 `holdgate`(전부 켜져야 함)를 쓰면 결과는 같지만, 나중에 레버를
 * 하나 더 붙였을 때 의미가 달라지므로 지금 의도를 그대로 적는 쪽을 골랐다.
 * 누군가 문을 넘어가면 그 뒤로 계속 열려 있다(obstacles.ts forceOpen) —
 * 눌러준 사람이 반대편에 영영 남지 않게 하는 안전장치다.
 *
 * [범퍼는 장애물이 아니라 사고 장치다]
 * -70의 범퍼 둘은 길을 막지 않는다. 공이 닿으면 엉뚱한 쪽으로 튀고, 달려와
 * 박은 사람은 넘어진다. "잘 피하면 아무 일도 없는" 것이 아니라 **공을 몰고
 * 지나가려 하면 반드시 한 번은 튄다**는 자리다.
 *
 * [왜 "패스존" 같은 전용 구역을 안 쓰나]
 * 협동을 특정 칸 안에서만 하게 만들면 그 칸 밖에서는 둘이 남남이 된다.
 * 여기서는 문이 코스 한복판을 막고 있고 발판이 그 코스 위에 있다 — 지나가는
 * 길 자체가 협동을 요구하므로 따로 구역을 표시할 필요가 없다.
 */
export const STAGE2_LEVERS: MapDef = {
  id: "s2-levers",
  name: "2. 양쪽 레버",
  blurb: "누가 눌러 주는 동안에만 열린다",
  timeLimit: 240,
  targetId: BALL_ID,
  targetName: "공",
  goal: { x: 0, z: -124, radius: 2.4, halfWidth: GOAL_HALF_W },
  spawns: [[-1.6, 10], [1.6, 10], [-3.6, 10], [3.6, 10]],
  // 첫 문(-20)을 배운 직후 방해꾼 하나가 따라붙는다. 스폰 자리는 넓은 구간이고
  // 주변 장애물(스위퍼 -76 / 문 -20·-52·-92 / 낙하물 -42)과 6m 이상 떨어져 있다.
  botSpawns: [[3.4, -30]],
  // 공을 걷어내며 따라붙는 역할. 여기서 공을 **가져가는** 도둑까지 쓰면
  // 레버 구간(둘이 갈라져 있는 자리)에서 회수가 사실상 불가능해진다 —
  // 도둑은 4번 스테이지 담당이다.
  botRoles: ["chaser"],
  ballSlots: [-28],
  checkpoints: [-26, -60, -100],
  floor: { size: 30, color: 0xc8f5d8, outside: 0xb4ecc9, hideOutside: true, hideFloor: true, noGround: true },
  fog: [0xd2f7e2, 68, 205],
  build: makeCourse({
    label: "s2-levers",
    startZ: 18, finishZ: -130, seed: 5150220,
    sections: [[18, -14, LANE_HALF], [-14, -70, LANE_HALF], [-70, -130, LANE_HALF]],
    gates: [[-14, 0x7bed9f], [-70, 0xffd166]],
    checkpoints: [-26, -60, -100],
    // [발판 위에 낙하물을 떨어뜨리지 않는다] buttongate의 발판은 문보다 3.6m
    // 앞(+Z)에 있다. 발판 위에서 버티는 쪽은 피할 수가 없으므로 낙하 지점은
    // 발판에서 6m 이상 띄운다 (-92 문의 발판은 -88.4다).
    hazards: [-4, -40, -108],
    gimmicks: [
      { kind: "buttongate", z: -20, arg: 0, phase: 0.0, note: "배우는 자리. 주변에 아무것도 없다" },
      { kind: "spinner", z: -36, arg: 4.2, phase: 0.8 },

      // ---- 떨어진 레버 + 그것이 여는 문 (이 스테이지의 핵심)
      //
      // 레버는 코스 왼쪽 끝, 문은 8m 앞 한복판. 밟은 쪽에서는 문이 안 보인다.
      // 바닥에 보라색 줄이 레버에서 문까지 이어져 그린다(maps/course.ts).
      {
        kind: "lever", z: -46, x: -5.6, link: "s2-far",
        params: { hold: 1, w: 2.4, len: 2.4 },
        note: "밟고 있는 동안만 켜진다. 발을 떼면 그 자리에서 문이 닫힌다",
      },
      {
        kind: "coopgate", z: -54, link: "s2-far",
        note: "레버가 켜져 있는 동안 열린다. 누가 지나가고 나면 계속 열려 있다",
      },

      { kind: "popup", z: -64, arg: -1, phase: 0.5 },

      // ---- 범퍼 둘 (환경 기믹)
      // 레인 폭이 14m인데 ±3.0에 세워서 가운데를 좁힌다. 가운데로 몰면 공이
      // 튀고, 가장자리로 돌면 스위퍼(-76)에 걸린다.
      { kind: "bumper", z: -70, x: -3.0, params: { r: 1.1 }, note: "공이 엉뚱한 쪽으로 튄다" },
      { kind: "bumper", z: -70, x: 3.0, params: { r: 1.1 } },

      { kind: "sweeper", z: -76, arg: 0, phase: 0.3 },
      { kind: "buttongate", z: -92, arg: 0, phase: 0.0, note: "지난 쪽이 곧바로 다음 압박을 만난다" },
      { kind: "piston", z: -106, arg: 1, phase: 0.6, note: "여기부터 골(-124)까지 18m는 비워 둔다" },
    ],
    // 공 전용 틈. 문 앞에서 공과 사람이 한 번 갈라져 있어야, 문이 열린 순간에
    // "누가 공을 가지고 있나"가 매번 달라진다.
    ballSlots: [-28],
  }),
};
