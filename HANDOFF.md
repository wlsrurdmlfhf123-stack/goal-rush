# HANDOFF — Goal Rush! (골 러시!)

> 작성 시점: 2026-08-30
> 작성 방법: `client/src/**`, `server/`, `test/`, `README.md`를 전부 읽고,
> `npx tsc --noEmit` / `npm test` / 헤드리스 물리 계측을 **실제로 돌려서** 확인한 내용만 적었다.
> 수치는 전부 이 시점에 직접 측정한 값이다.

---

## 0. 지금 상태 한 줄

**2인 온라인 협동 물리 축구 「Goal Rush!」 — 3스테이지 코스가 처음부터 끝까지 플레이 가능한 상태.**
드리블/킥/개인기/봇/장애물/협동 관문/골 판정이 전부 붙어 있고, 타입체크와 테스트 199개가 전부 통과한다.
다음 단계는 **드리블 손맛 개선**이며, 그 뒤에 킥, 그 뒤에 물리적 공격(친구 날리기)으로 확장한다.

```
npx tsc --noEmit   → exit 0
npm test           → 199 passed, 0 failed
                     (net 29 / room 28 / ragdoll 39 / swap 11 / explosion 7 / game 44 / ball 41)
```

---

## 1. 게임 개요

- **목표**: 축구공(소품 id `90` = `BALL_ID`)을 코스 끝의 **골라인 안으로 굴려 넣는다**.
- **골 판정**: 거리 판정이 아니라 **라인 통과 판정**이다 (`game.ts` `checkCross`).
  공의 z가 한 스텝 안에 `goalZ`를 앞→뒤로 가로지르고, `|x - goalX| <= halfWidth(4.2)`,
  `y <= 3.0`, 그리고 **안고 있지 않아야** 골이다.
- **제한시간**: 맵별 200 / 230 / 260초. 초과하면 실패.
- **인원**: 방 하나에 최대 4명. 실제 설계 기준은 2인.
- **「서로조종」**: 사람이 2명 이상이면 **내 입력이 다음 사람의 캐릭터를 조종한다**
  (`controlTargetOf`). 봇은 조종 대상에서 제외된다.

### 스테이지 (`maps.ts` `MAPS`)

| # | id | 이름 | 골 위치 | 제한시간 | 봇 | 대표 기믹 |
|---|----|------|---------|----------|----|-----------|
| 1 | `sky` | 하늘 코스 | z=-104 | 200s | 1 | 튜토리얼 패드, 낙하물, 회전봉/스위퍼 |
| 2 | `canyon` | 회전 협곡 | z=-134 | 230s | 2 | 공 전용 틈, 좁은 다리(반폭 2.6), 버튼 문 |
| 3 | `denof` | 봇 소굴 | z=-154 | 260s | 3 | 셔터, 봇 셋, 조합 장애물 |

레인 반폭 `LANE_HALF = 7`, 골대 반폭 `GOAL_HALF_W = 4.2`.
`LEGACY_MAPS`(집/창고/옥상)는 **냉장고 옮기기 시절의 맵**이다. 게임에 등록되지 않고,
가구 빌더의 유일한 사용처 겸 물리 회귀 확인용으로 남겨 뒀다.

### 조작 (`index.html` #help 기준, 실제 코드와 일치 확인)

| 키 | 동작 |
|----|------|
| WASD | 이동 |
| Space | 점프 |
| **F** | 킥 (누른 시간만큼 차징, **뗄 때** 나간다). 공이 `kickRange`(1.85m) 밖이면 **러시** |
| **E** | 공 들기 / 놓기 (손이 안 닿으면 줍는 동작 `scoop` 시작) |
| **Shift** | 개인기 1 — 옆으로 재끼기 (공은 한쪽, 몸은 반대쪽) |
| **Q** | 개인기 2 — 스톱턴 (급정지 + 뒤로 빠지기) |
| M / H | 음소거 / 디버그 HUD |

---

## 2. 파일 구조 (실제 상태)

```
client/src/
  main.ts       3053줄  물리 루프, 입력, grab, 동기화, 연출 배관, 상황 힌트, 디버그 훅
  ragdoll.ts    1405줄  15바디 액티브 래그돌 (서기/걷기/넘어짐/복구)  ★건드리지 말 것
  maps.ts       1315줄  MAPS 3개 + LEGACY_MAPS 3개 + makeCourse() 코스 생성기
  ball.ts       1225줄  드리블 / 킥 / 러시 / 줍기 / 개인기 2종      ← 지금 작업 대상
  mapkit.ts      936줄  팔레트 + 가구·소품·코스 부품 빌더
  obstacles.ts   757줄  회전봉/피스톤/거대공/스위퍼/팝업/셔터/협동게이트/버튼문
  world.ts       699줄  Three 씬 + cannon 월드, 맵 로드·해제
  game.ts        534줄  타이머 / 골 판정 / 결과 화면 / 맵 진행
  audio.ts       319줄  WebAudio 합성 SFX 16종 (샘플로 교체 가능한 구조)
  menu.ts        300줄  타이틀 / 캐릭터 선택 / 방 만들기·참가 / 대기실
  hazards.ts     243줄  위에서 떨어지는 큰 공
  carry.ts       213줄  들기(수직)/밀기(수평) 힘 예산 분리
  bot.ts         205줄  방해꾼 AI (치팅 없음 — 공 위치만, 그것도 0.3초 지연)
  fx.ts          162줄  파티클 풀 (touch / trail / dash / kick)
  net.ts         130줄  WebSocket 래퍼
  protocol.ts    123줄  메시지 타입
  shapes.ts       98줄  반두께/표면점 (Sphere 포함)
  input-math.ts   96줄  카메라 기저 / 마우스 룩 / 충돌 그룹 (순수 함수)
  characters.ts   44줄  캐릭터 프리셋 8개
server/server.js 246줄  ws 릴레이 (물리 계산 없음). 방 단위 격리
```

### 개발용 임시 파일 (지우면 브라우저 검증이 불가능해진다)

- `client/_tmp-harness.html` — 자동 플레이테스트용 페이지
- `client/_tmp-auto.js` — **가상 시계 + rAF 수동 펌프 + 오토파일럿**
  (`__pump` / `__k` / `__auto` / `__goto` / `__stage`)
- `client/_tmp-net.js` — 2인 네트워크 검증 하네스 (MessageChannel로 이벤트 루프 양보)
- `test/.diag*.ts`, `test/.build/*.mjs` — 지난 계측의 잔재. 지워도 무방
- `test/bug-repro-test.ts` — `npm test`에 **등록되어 있지 않다**

---

## 3. 구현되어 동작하는 기능

### 물리 / 캐릭터
- 15바디 액티브 래그돌. 라이드하이트 스프링 + 자세 유지 토크 + 속도 추종 이동.
- 넘어짐 3단계: `ACTIVE → RAGDOLL → RECOVERING`. 기울기 지속(0.55s) 또는 코어 충격(13 m/s)으로 전환.
- `knockdown(seconds)` 공개 API. 넘어지는 순간 속도를 VCAP 9 / WCAP 7로 깎아 "폭발"을 막는다.
- 2.8등신 SD 렌더 비율(`VIS`). **충돌체(`DIM`)는 그대로**라 물리 게인이 유지된다.

### 공 (`ball.ts`)
- **드리블 = 터치 방식.** 상시 견인(자석)이 없다. 발이 닿는 상자
  (앞 `touchAhead` 1.7 × 옆 `touchSide` 1.6) 안에 들어왔을 때만
  `pokeTimer`(0.13~0.22s, 속도 연동) 간격으로 충격량 한 방.
- 발 앞 목표 거리가 속도 연동(`leadBase` 0.62 + 속도 × `leadPerSpeed` 0.22).
- 꺾는 동안 **닿는 범위만** 회전 속도에 비례해 넓어진다(`turnReach*`, `turnBehind`).
  끌어당기는 힘이 아니라 "찰 자격"만 넓히므로 자석이 되지 않는다.
- 터치 사이의 약한 **옆 방향** 유도만 있다(`guideAccel` 5.5 / `guideDamp` 2.0).
  진행 방향에는 절대 걸지 않는다.
- 킥: 차징(0.55s) → 충격량 6.5~15.5 N·s + 위로 1.6~4.4. **조준(카메라) 방향**으로 나간다.
- 러시: 공이 킥 사거리 밖이면 F가 러시가 된다. 몸 전체를 질량 비례로 밀고(45 m/s²),
  발이 닿는 범위가 2.2배, 러시 터치는 미는 게 아니라 **죽인다**(`rushTouchKeep` 0.25).
  대신 러시 중에는 몸을 거의 못 튼다(`rushSteer` 0.22) = 직선 돌파라는 대가.
- 개인기 1 (Shift): 공을 띄워 한쪽으로, 몸은 반대쪽으로. 좌우 입력으로 방향을 고른다.
- 개인기 2 (Q): 급정지 + 공을 발밑에 세우고 뒤로 빠진다.
- 줍기(E): `scoopRange` 2.8m 안에서 0.55초 동안 공을 가슴으로 당긴다. 안고 뛰면 느려진다.

### 코스 / 장애물
- 낙하물(`hazards.ts`), 회전봉·피스톤·거대공·스위퍼·팝업·셔터(`obstacles.ts`).
- **어느 것도 플레이어 좌표를 읽지 않는다.** 위상과 시드 난수로만 움직인다 = 보고 피할 수 있다.
- 장애물 피격 판정은 kind별로 명시적이다(`KNOCKS` 집합 + `insideBody`).
  `hitMinSpeed` 1.2 / `hitMinY` 0.62 / `hitCooldownTime` 2.4로
  **일어나는 도중 재피격되는 무한 루프**를 막는다.
- `popup`은 일부러 `KNOCKS`에서 제외했다 — 뒤로만 밀어서 진행이 사라지기 때문.

### 협동 장치
- **협동 게이트(`coopgate`)**: 한 명이 찬 공을 다른 사람이 받으면 열린다
  (`updateCoopPass`: 4.5m 이상 날아가고 5초 안에 반경 2.6m로 들어와야 한다).
- **버튼 문(`buttongate`)**: 좌우 발판을 둘이 **동시에** 밟고 있는 동안만 열린다.
- 둘 다 **싱글에서는 자동으로 열어 둔다**(`syncCoopGates`).
- **서로 부딪히기(`updatePlayerBumps`)**: 사람끼리 1.15m 안에서 접근 속도 3.2 m/s 이상이면
  **둘 다 넘어진다**. 봇은 제외(난이도만 올라가서 뺐다).

### 안전장치 / 구제
- 낙사 리스폰, 코스 아래 끼임 구제(`WEDGE`), 제자리 끼임 구제(`STUCK`),
  공 전용 틈에 낀 공 빼내기(`updateSlotUnstick`),
  **공 되밀림 제한**(`updateBallBackstop`: 도달 최전선에서 25m 이상 뒤로는 안 간다).

### 연출
- 파티클(터치/잔상/대시/킥), 화면 흔들림, FOV 부스트, 킥 게이지,
  기술 이름 토스트, 상황 대사(`dramaLine`), 경고 배너, 카운트다운,
  화면 밖 공/친구 위치 표시(`ball-cue` / `mate-cue`), 머리 위 표식.
- SFX 16종 WebAudio 합성.

### 네트워크
- 방(4자리 코드) 단위 완전 격리. 첫 접속자가 host.
- **host만 물리를 돌린다. 비-host는 모든 바디가 KINEMATIC인 얇은 클라이언트다.**
- 입력 30Hz 전송, 스냅샷에 래그돌 15바디 + 소품 + 게임 상태 + SFX 이벤트를 얹어 보낸다.
- host 이양 지원. 캐릭터 프리셋 중복 선택은 서버가 거절한다.

---

## 4. 테스트 결과 (직접 실행함)

| 스위트 | 개수 | 결과 | 무엇을 잠그는가 |
|--------|------|------|-----------------|
| `test:net` | 29 | PASS | 접속/ID/host 이양, 입력이 host에게만, 스냅샷 권위 위조 방지 |
| `test:room` | 28 | PASS | 방 코드 생성, **다른 코드끼리 안 섞인다**, 늦게 들어와도 합류 |
| `test:ragdoll` | 39 | PASS | 스스로 서기, 이동, 넘어짐/복구, 자기 파츠 비충돌, 수치 안정성 |
| `test:swap` | 11 | PASS | 「서로조종」 매핑, P1 입력이 P2 몸을 움직이는가 |
| `test:explosion` | 7 | PASS | dt 스파이크(0.5s)에도 관절이 발산하지 않는가 (solver 22 + dt clamp) |
| `test:game` | 44 | PASS | 타이머, **골라인 통과 판정**, 오판정 방지, 다시하기, 권위 분리 |
| `test:ball` | 41 | PASS | 드리블이 발 앞에 따라오는가, **자석이 아닌가**, 개인기, 킥 조준 |
| **합계** | **199** | **0 failed** | |

> README에 적힌 "전체 147개"는 낡은 숫자다.

---

## 5. 오늘 직접 측정한 물리 수치 (다음 작업의 기준선)

헤드리스 rig(`test/ball-test.ts`와 같은 월드 설정)로 계측.

### 드리블 — 직진 4초

| 상황 | 사람 속도 | 공 최고속 | 최대 간격 | 사거리 밖 |
|------|-----------|-----------|-----------|-----------|
| 혼자 | 4.60 m/s | 6.24 | 1.60 m | 0% |
| 둘이 나란히 (좌우 1.8m) | 4.59 | 6.17 | 1.84 m | 0% |
| 둘이 나란히 (좌우 0.9m) | 4.60 | 6.06 | 1.67 m | 0% |

→ **두 사람이 같은 공에 동시에 터치를 넣어도 공이 폭주하지 않는다.**
  걱정했던 "협동 시 드리블이 뭉개진다"는 지금 구조에서는 일어나지 않는다.
  다만 뒤집어 말하면 **친구가 바로 옆에 있어도 아무 일도 안 일어난다** — 3단계에서 손댈 지점.

### 킥 세기 → 실제 공 속도

| 상황 | 결과 |
|------|------|
| 정지한 공, power 0 (톡) | 3.90 m/s |
| 정지한 공, power 0.5 | 6.76 m/s |
| **정지한 공, power 1.0 (풀차지)** | **9.63 m/s** |
| **드리블 중인 공, power 1.0** | 4.51 → **13.89 m/s** |

→ 이론상 15.5 N·s / 1.1 kg = 14.1 m/s인데 **정지한 공은 9.63밖에 안 나온다.**
  바닥에 붙어 있는 공은 첫 스텝의 접촉 마찰이 충격량을 크게 먹는다.
  결과적으로 **"멈춰서 자세 잡고 세게 차기"가 "달리면서 차기"보다 약하다.**
  세트피스 슛이 약하게 느껴지는 원인이다.

### 넘어뜨리는 임계값

| 검사 | 결과 |
|------|------|
| 가슴에 공 직격 12 m/s | 안 넘어진다 |
| **가슴에 공 직격 13 m/s** | **넘어진다** (`P.impactSpeed = 13`) |
| 전속력 몸통박치기 (충격량만, `updatePlayerBumps` 없이) | 밀린 거리 **0.00 m**, 둘 다 ACTIVE |

→ **핵심 발견 1**: 드리블 중 풀차지 킥(13.89 m/s)은 **이미 사람을 넘어뜨릴 수 있다.**
  "공으로 친구 때리기"는 새 시스템 없이 이미 물리적으로 성립한다 (아슬아슬하게).

→ **핵심 발견 2**: `control()`의 속도 서보(`moveAccel` 8.0 / `moveForce` 980N / `brakeRatio` 1.0)가
  **넉백을 그 자리에서 지운다.** 20kg에 최대 49 m/s²로 제동을 걸기 때문이다.
  **그래서 3단계 공격은 충격량만으로는 절대 안 된다.**
  반드시 (a) `knockdown()`으로 제어를 끊거나, (b) `dashDir()`처럼 이동 입력 자체를 덮어써야 한다.
  실제 게임에서 몸통박치기가 먹히는 건 `updatePlayerBumps`가 `knockdown()`을 부르기 때문이다.

---

## 6. 알려진 버그 / 미완성 (전부 코드에서 확인함)

### B1. 튜토리얼 안내가 **절대 안 뜬다** — 실제 버그
`main.ts:1355`
```ts
if (!target || !inGame || world.map.id !== "goalrush") { elTut.hidden = true; return; }
```
`"goalrush"`라는 id를 가진 맵은 **없다**. 실제 id는 `sky` / `canyon` / `denof`.
바닥의 키 패드(`TUTORIAL_PADS` z = 6 / -1 / -6 / -11)는 `sky`에 정상적으로 그려지는데,
그 위에 서도 HUD 한 줄 설명(`TUTORIAL_TEXT`)이 한 번도 안 나온다.
→ `world.map.id !== "sky"`로 고치거나, `MapDef`에 `tutorial?: boolean`을 올려 그걸 보는 편이 낫다.

### B2. README.md가 통째로 옛 게임 문서다
「Hold Tight! (꽉 잡아!)」 **냉장고 옮기기** 시절 그대로다.
게임 규칙, 맵 3개(집/창고/옥상), 조작표(WASD/Space/E만),
파일 목록(ball/bot/fx/audio/hazards/obstacles가 아예 없음), 테스트 개수(147) 전부 현재와 다르다.
**새로 오는 사람이 이걸 먼저 읽으면 완전히 잘못된 그림을 갖는다.**
단, 아래 절들은 **여전히 유효하고 가치가 높다** — 재작성할 때 반드시 보존할 것:
「맵 전환에서 조심할 것」 / 「잡기 모션(grab reach)」 / 「캐릭터 외형」 /
「물리 엔진에서 조심할 것」 / 「빌드 주의(tsc exit code)」.

### B3. `judge` 관련 주석이 낡았다 (동작은 정상)
`game.ts:129` / `maps.ts:106` 주석은 "Goal Rush 코스는 골 판정이 5단계 몫이라 아직 `judge: false`다"라고 말한다.
실제로는 **어느 맵도 `judge`를 세우지 않고**, `judging = def.judge !== false`이므로 **판정은 켜져 있다.**
골 판정·타이머·결과 화면 전부 정상 동작한다(`test:game` 44개가 이걸 잠근다). 주석만 틀렸다.

### B4. `game.ts`의 죽은 코드
`dwell` / `GOAL_DWELL`이 남아 있고 `void dwell; void GOAL_DWELL;`로 무마돼 있다.
"출구 안에 0.25초 머무르면 성공"이 라인 통과 판정(`checkCross`)으로 바뀌면서 쓸모가 없어졌다.

### B5. 정지한 공의 킥이 설계 의도보다 훨씬 약하다
5절 측정 참고 (9.63 vs 13.89 m/s). 버그라기보다 **미조정**이지만,
"멈춰서 조준하고 세게 차기"라는 플레이가 성립하지 않는다는 뜻이라 2단계(킥 감각)에서 다뤄야 한다.

### B6. `laneHalf`가 7로 하드코딩되어 있다 (잠재)
`main.ts`에서 `createHazards(world, 7)` / `createObstacles(world, 7)` / `createBots(7)`.
그런데 `canyon`의 다리 구간은 반폭 `BRIDGE_HALF = 2.6`이다.
지금은 봇 스폰(z -50 / -112)과 낙하물(z -4 / -34 / -108)이 전부 다리 밖(-56~-96)이라 드러나지 않는다.
**다리 구간에 봇이나 낙하물을 배치하는 순간 허공에서 헛돈다.**

### B7. `test/bug-repro-test.ts`가 `npm test`에 등록돼 있지 않다
`package.json`의 `test` 스크립트는 net/room/ragdoll/swap/explosion/game/ball 7개만 돌린다.

---

## 7. 절대 건드리면 안 되는 코드

> 전부 "실측으로 데여서 지금 값이 된" 자리다. 각 파일 주석에 그 경위가 적혀 있다.

### 7-1. `ragdoll.ts`의 `control()` — 최우선
서기/걷기/넘어짐/일어남이 전부 여기서 나온다.
**`ball.ts` 전체가 "control()은 안 건드린다"를 전제로 설계됐다.**
공을 조작하고 싶으면 두 가지만 한다:

1. 공과 골반에 **힘·충격량을 더한다**
2. **"무엇을 이동 입력으로 줄 것인가"를 바꾼다**
   (`ballPlay.dashDir()` / `rushDir()` → `main.ts`가 `e.input`을 덮어쓴다)

개인기의 옆이동 충격량을 30 → 110으로 3.7배 올려도 옆 이동이 0.36 → 0.39m로 거의 안 변했다.
`control()`이 속도 서보라 도로 끌어오기 때문이다. **입력을 바꾸는 쪽이 유일하게 먹히는 길이다.**

### 7-2. `P` (ragdoll 파라미터)와 `DIM` (충돌체 치수)
`rideHeight` 0.86 / 관절 pivot / 총질량 / 관성모멘트가 전부 여기서 나오고
그 위에 서기·걷기 게인이 실측으로 맞춰져 있다.
**외형을 바꾸려면 `VIS`(렌더 메시 배수)만 만진다.**
특히 **다리 길이를 줄이면 골반이 rideHeight에 있을 때 발이 바닥에 안 닿아 캐릭터가 공중에 뜬다.**

- `P.endDamp`는 **반드시 `0 <= d < 1`**. cannon의 angularDamping은 `pow(1-d, dt)`라
  1 이상을 넣으면 그 자리에서 NaN이다 (3.0을 넣어 첫 스텝부터 NaN 복구가 쏟아진 적 있음).

### 7-3. `world.ts`의 물리 월드 설정
- **`SAPBroadphase` 금지, `NaiveBroadphase`만.** SAP는 정적 바디 AABB가 원점에 굳어(236개 중 194개)
  스윕이 엉뚱한 데서 끊기고 **캐릭터가 소품을 그대로 통과했다.**
- **`solver.iterations = 22` + 초반 dt clamp + substep 상한.** `test:explosion`이 이 값을 잠근다.
- **마찰 상한은 "힘"이 아니라 "한 스텝 충격량"이다.** 원하는 실효 계수가 있으면 `μ = μ_eff × dt`로 넣어야 한다.

### 7-4. 네트워크 권위 구조
- `setAuthority()`: 비-host는 **모든 바디가 KINEMATIC, mass 0**이다.
  → **`fixedUpdate`의 host 분기 안에 넣은 연출은 친구 화면에 존재하지 않는다.**
    비-host에게도 보여야 하는 연출은 `updateLocalSfx` / `watchGatesLocally`처럼
    **스냅샷을 관찰해서 로컬로** 만들거나, `netSfx()`로 실어 보내야 한다.
- **엣지 입력(`grabEdge`/`trickEdge`/`stopEdge`/`kickEdge`)은 host일 때만 `fixedUpdate`에서 지운다.**
  비-host에서 여기서 지우면 **전송되기 전에 사라져서 친구의 F/Shift/Q/E가 전부 씹힌다**
  (실측으로 재현된 버그). 비-host는 `sendNetwork()`가 지운다.
- 게임 규칙(`game.ts`)은 **물리 바디를 하나도 만들지 않는다.** 마커는 전부 장식 메시다.
  월드 리셋은 `main.ts`가 훅으로 제공한다 — 제약을 남긴 채 바디를 텔레포트하면 그 프레임에 물리가 폭발한다.

### 7-5. `ball.ts`의 `B` 상수 — **소수점 최적화 금지**
주석에 명시돼 있다: 같은 설정으로 지그재그 유지율을 세 번 재도 **0.09 / 0.26 / 0.65**로 흔들린다.
꺾는 순간에 터치가 마침 들어오느냐로 갈리기 때문이고, **그게 이 드리블의 의도된 실수 여지다.**
대략적인 영역만 맞추고, 시행 1회의 숫자로 값을 바꾸지 말 것.

### 7-6. 맵 전환
- `mapkit.ts`의 `staticBody()`가 만든 바디는 `Build.bodies`에 모아 두고 맵을 내릴 때 전부 `removeBody`.
  안 빼면 **보이지 않는 예전 맵 벽**에 부딪힌다.
- **지오메트리/머티리얼은 dispose 하지 않는다** (`boxGeo/cylGeo/sphGeo/toyMat`은 맵끼리 공유하는 캐시).
  반대로 `game.ts`의 마커는 캐시를 안 쓰므로 **반드시** dispose.
- `objects` / `objectById`는 **새로 만들지 않는다.** 제자리에서 비우고 다시 채운다.

### 7-7. 빌드
```bash
npx tsc --noEmit; echo "TSC_EXIT_CODE=$?"   # 0이 아니면 vite build를 해도 dist는 안 바뀐다
```
`build`가 `tsc --noEmit && vite build`라, 타입 에러가 하나라도 있으면 **dist에 옛 번들이 그대로 남는다.**
"소스는 고쳤는데 브라우저에서 옛날 버그가 계속 보이는" 상황이 실제로 있었다.

### 7-8. 브라우저 검증 방법
숨겨진 탭은 Chrome intensive throttling으로 rAF가 2~9Hz까지 떨어진다.
`_tmp-auto.js`가 **`performance.now`를 가상 시계로 바꾸고 rAF를 직접 펌프**해서
정확히 1/60 dt로, 스로틀과 무관하게 돌린다.
**스크립트를 설치한 직후 스크린샷을 한 번 찍어 rAF를 인계받아야 한다.**
멀티 검증(`_tmp-net.js`)은 펌프 사이에 **MessageChannel로 이벤트 루프에 양보**해야
WebSocket 메시지가 배달된다 (setTimeout은 숨겨진 탭에서 1초 이상 throttle된다).

---

## 8. 최근 내린 디자인 결정

### D1. 「냉장고 옮기기」 폐기 → 「Goal Rush!」로 피벗
2인 협동 물리 코미디라는 축은 유지하되, 목표를 **공을 몰고 골까지**로 바꿨다.
옛 맵은 `LEGACY_MAPS`로 보존(가구 빌더의 유일한 사용처 + 물리 회귀 확인용).

### D2. 드리블은 **자석이 아니다**
상시 견인 PD를 없애고 터치 방식으로 바꿨다. 터치와 터치 사이에 공은 순수하게 굴러간다.

- 직진 → 툭툭 차며 굴려 간다
- 급회전 → 공은 관성으로 흘러간다. 다음 터치가 닿아야 꺾인다
- 급정지 → 공만 굴러 나간다 (아주 가깝고 느릴 때만 발로 세울 수 있다)

**"유도를 줄이는 것"은 답이 아니었다.** `guideAccel` 4.5→1.6, `guideDamp` 1.5→0으로 줄였더니
개선이 아니라 조작 불능이었다(지그재그 5초에 발 앞 1.37→5.32m, 프레임의 55%가 사거리 밖).
진짜 원인은 **발 앞 목표거리가 고정이고 터치 간격이 일정한 것**이었고,
그쪽을 속도 연동으로 바꾸는 게 해법이었다.

### D3. 회전은 "절벽"이 아니라 "경사"여야 한다
90도 회전이 동전던지기였던 원인은 `alongDist < behindLimit(-0.4)`에 걸리면
그 뒤로 **매 프레임 즉시 return**해서 터치가 영영 안 나가는 것이었다(실측 n=20에서 40% 실패).
→ **끌어오는 힘을 더하지 않고, 회전 속도에 비례해 "찰 수 있는 자격"만 연속적으로 넓힌다.**
  충격량 상한도, `turnBite`도, 터치 간격도 그대로다. 직진이면 회전 속도가 0이라 예전과 완전히 같다.

### D4. 「물리 코미디」를 핵심 재미로 삼는다 — 다만 **순서를 지킨다**

> 단순한 2인 협동 축구가 아니라, **협동해서 골 넣으려다가 서로 방해하고, 물리적으로 날아가고,
> AI한테 공 뺏기고, 결국 웃기는 상황이 발생하는 게임**

지향하는 상호작용:

1. **친구 공격** — 몸통박치기 / 드롭킥 / 태클 / 점프해서 들이받기 / 넘어져 구르며 맞히기
2. **축구공으로 친구 공격** — 강하게 찬 공이 친구에게 직격 → 뒤로 날아감
3. **AI도 동일한 물리 상호작용 대상** — 밀기 / 부딪히기 / 공에 맞기 / 넘어지기 / 날아가기

**하지만 지금 당장 공격 시스템을 구현하지 않는다.** 드리블 물리부터 제대로 만든 다음 붙인다.
공격이 드리블과 **같은 물리 기반**으로 작동해야 시스템이 자연스럽게 연결되기 때문이다:
드리블 → 킥 → 강한 킥 → 친구에게 직격 → 친구가 날아감.

### D5. 공격의 보상은 **데미지가 아니라 물리적 결과**다
HP도 점수도 아니다. 넘어짐 / 밀려남 / 회전 / 래그돌 / **공 소유권 변화** / 예상 못 한 위치로 날아감.
경쟁적인 격투게임처럼 만들지 않는다. **"친구를 날려버렸더니 웃긴 상황이 발생하는 시스템"**이다.
드롭킥으로 넘어뜨린 친구가 넘어지면서 공을 막아주는 것 같은 **예상 못 한 결과도 좋은 결과**로 친다.

### D6. 기존 구조를 갈아엎지 않는다
host authority / 스냅샷 동기화 / cannon-es / `pokeTimer` / 기존 ragdoll / 기존 carry / 기존 패스 게이트를
최대한 활용한다. 새 시스템을 만들기 전에 **재사용 가능한 물리·래그돌·충돌 로직을 먼저 찾는다** (→ 9절).

### D7. 진행 방식
**각 단계마다 분석 → 설계 → 추천안까지만 제시하고, 승인 전에는 코드를 수정하지 않는다.**

---

## 9. 3단계(물리적 공격)에서 재사용할 수 있는 기존 자산 — 조사 완료

새로 만들어야 할 것보다 **이미 있는 것이 훨씬 많다.**

| 필요한 것 | 이미 있는 것 | 위치 |
|-----------|--------------|------|
| 넘어뜨리기 | `rag.knockdown(seconds)` — 공개 API. 속도 캡까지 처리 | `ragdoll.ts:697` |
| 충돌로 자동 넘어짐 | head/torso/pelvis `collide` 리스너, 상대속도 > 13 m/s | `ragdoll.ts:690` |
| **공 직격 넉다운** | **위 리스너로 이미 성립한다** (실측 13 m/s부터. 드리블 중 풀차지 킥 = 13.89) | 5절 측정 |
| 사람↔사람 충돌 넉다운 | `updatePlayerBumps` — 거리 1.15 / 접근속도 3.2 / 쿨다운 1.6 / 양쪽 다 넘어짐 | `main.ts:1578` |
| "무엇이 무엇을 넘어뜨리는가" 판정 틀 | `KNOCKS` 집합 + `insideBody()` + `hitCooldown`/`hitMinY`/`hitMinSpeed` | `obstacles.ts` |
| 넉백 파라미터 패턴 | `knockSide` / `knockUp` / `knockdownTime` / `hitCooldown` | `hazards.ts`, `obstacles.ts` |
| 몸 전체를 질량 비례로 미는 추진 | 러시의 `tick()` — **골반만 밀면 걸음이 무너진다**는 실측 결론까지 포함 | `ball.ts` |
| control()을 안 건드리고 이동을 덮어쓰기 | `dashDir()` / `rushDir()` → `main.ts`가 `e.input`에 대입 | `ball.ts`, `main.ts:2550` |
| 접촉으로 상태를 깨는 통로 | 봇의 `brokeCarry` 반환 → host가 캐리를 푼다 | `bot.ts`, `main.ts:2528` |
| 연출 배관 | `fx.dash/kick/touch`, `netSfx`, `dramaLine`, `showMove`, `addShake` | `fx.ts`, `main.ts` |

**설계상 반드시 지켜야 할 제약 (5절 측정에서 확정):**
공격은 **충격량만으로는 아무 일도 일어나지 않는다.** `control()`의 속도 서보가 즉시 지운다.
반드시 `knockdown()`으로 제어를 끊거나, `dashDir()` 방식으로 이동 입력 자체를 덮어써야 한다.

---

## 10. 다음 작업 우선순위

### 🔴 1순위 — 드리블 감각 (현재 진행 중, **분석/설계 단계**)
아직 코드 수정 승인 전이다.
다뤄야 할 축: 공이 몸에서 떨어지는 거리 / 공이 몸을 따라오는 느낌 / 방향 전환 /
가속·감속 / 공의 관성 / 터치 간격 / 공을 밀어내는 거리 / 공을 다시 컨트롤하는 느낌.

**기준선은 5절에 있다.** 지금 수치상으로는 "놓치지 않는다"가 지나치게 잘 지켜지고 있다
(사거리 밖 0%, 최대 간격 1.6~1.84m). 즉 **드리블이 안전한 대신 밋밋할 가능성**이 높다.
숫자만 보고 판단하지 말고 브라우저에서 손맛을 먼저 확인할 것(7-8 참고).

### 🟠 2순위 — 킥 감각
약하게 밀기 / 일반 킥 / 강한 킥, 그리고 **킥 방향과 플레이어 방향의 관계**.
**B5(정지한 공의 킥이 9.63 m/s밖에 안 나오는 문제)를 여기서 해결한다.**

### 🟡 3순위 — 물리적 공격
플레이어↔플레이어 / 공↔플레이어 / 플레이어↔AI / 공↔AI.
9절의 자산을 먼저 쓰고, 새 시스템은 최소한으로.
**협동을 완전히 망가뜨리지 않도록**, 친구를 방해하는 상황과 도와주는 상황이 **양쪽 다** 나오게 설계한다.

### 🟢 잡일 (언제든)
- **B1 튜토리얼 안내 버그** — 한 줄이고 실제로 기능이 죽어 있다. 우선 처리 권장
- **B2 README 재작성** — 새 사람이 잘못된 문서를 먼저 읽는다. 7절의 "조심할 것"들은 반드시 보존
- B3 낡은 `judge` 주석 정리 / B4 `dwell` 죽은 코드 제거
- B6 `laneHalf` 하드코딩을 맵에서 읽도록
- B7 `bug-repro-test`를 `npm test`에 넣거나 지우기
- `test/.diag*` 잔재 정리

---

## 11. 실행 / 검증

```bash
npm install

# 터미널 1 — WebSocket 릴레이 (포트 8080). 싱글만 할 거면 불필요
npm run server

# 터미널 2 — 클라이언트 (포트 5173)
npm run dev
```

멀티: 탭 A에서 [방 만들기] → 나온 4자리 코드를 탭 B의 [방 참가하기]에 입력.
다른 기기: `http://<PC_IP>:5173`
(클라이언트가 접속한 hostname 기준으로 `ws://<hostname>:8080`에 붙는다).

```bash
npx tsc --noEmit; echo "TSC_EXIT_CODE=$?"   # ★ 빌드 전 항상 확인 (7-7)
npm test                                    # 199개
npm run test:ball                           # 공만 빠르게
npm run build
```

브라우저 안에서 상태를 들여다볼 때는 `window.__dbg` (`DEBUG = true`일 때만 붙는다):
`__dbg.ball()` / `__dbg.players()` / `__dbg.bots()` / `__dbg.hazards()` /
`__dbg.ballConst`(= `B`, 값을 바꿔가며 손맛 조절) / `__dbg.teleport(x, z)` /
`__dbg.setBots(n)` / `__dbg.pressKick()` 등.

---

## 12. 맵 / 스테이지 구조 (2026-08-31 개편)

> 2절의 파일 구조표와 1절의 스테이지 표는 이 절이 최신이다.
> 물리 / 래그돌 / 네트워크 / `ball.ts`는 **하나도 건드리지 않았다.**

### 12-1. 파일이 나뉘었다

`maps.ts`(1315줄) 하나였던 것을 아래로 쪼갰다. `import ... from "./maps"`는
전부 그대로 동작한다 — `maps/index.ts`가 예전과 같은 것을 다시 내보낸다.

```
client/src/maps/
  index.ts     공개 창구 (world/main/game는 여기만 안다)
  types.ts     MapDef / MapCtx / 소품 id 규약
  course.ts    Goal Rush 코스 생성기 (지형·난간·낭떠러지 경고·장식·골대)
  gimmicks.ts  기믹 선언 어휘 + 구현 여부 레지스트리   ← 기믹 담당과의 접점
  legacy.ts    냉장고 시절 맵 3개 (등록되지 않음)
client/src/stages/
  index.ts               MAPS / STAGES
  roadmap.ts             10 스테이지 계획표 (STAGE_PLANS)
  stage01-warmup.ts      1. 몸풀기
  stage02-levers.ts      2. 양쪽 레버
  stage03-movingfloor.ts 3. 움직이는 바닥
  lab.ts                 기믹 시험장 (`?lab`)
  archive.ts             예전 2·3번(회전 협곡 / 봇 소굴) 보관
```

### 12-2. 등록된 스테이지

| # | id | 이름 | 골 | 제한 | 협동이 나오는 자리 |
|---|----|------|----|------|-------------------|
| 1 | `s1-warmup` | 몸풀기 | -104 | 190s | 공 전용 틈(-56)에서 공과 사람이 갈라진다 |
| 2 | `s2-levers` | 양쪽 레버 | -124 | 230s | 버튼 문 3개(-20/-50/-92) — 밟고 있는 동안만 열린다 |
| 3 | `s3-movingfloor` | 움직이는 바닥 | -124 | 240s | 세 갈래 + 낭떠러지 + 외줄 다리. 길이 셋인데 공은 하나 |

예전 `sky`는 1번으로 이어졌고(협동 장치를 빼서 진짜 몸풀기로 만들었다),
`canyon`/`denof`는 `stages/archive.ts`로 옮겼다. **지운 것은 없다** —
주소에 `?lab`을 붙이면 시험장과 함께 로드된다.

### 12-3. 기믹 담당(Claude A)에게 — 새 kind를 붙이는 방법

맵 쪽은 이미 준비되어 있다. 할 일은 두 가지뿐이다.

1. `obstacles.ts`의 `ObstacleKind`에 kind를 넣고 동작을 구현한다
2. `maps/gimmicks.ts`의 `GIMMICK_STATUS`에서 그 kind를 `"live"`로 바꾼다

그러면 이미 배치돼 있던 스테이지가 그대로 살아난다. 스테이지 파일은 손댈
필요가 없다 — 좌표와 파라미터가 처음부터 적혀 있다.

- 아직 없는 kind는 `drawGimmickPlaceholder()`가 **바닥 표식만** 그린다
  (물리 없음 = 길을 막지 않는다). 그래서 지금도 전 스테이지가 완주된다.
- 파라미터가 둘 이상인 기믹은 `GimmickSpec.params`(자유 사전)를 쓴다.
  `addObstacle(id, kind, z, arg, phase)`의 `arg` 하나로는 부족하기 때문이다.
  런타임이 붙을 때 `AddObstacle` 시그니처에 `params`를 얹으면 된다.
- **파라미터 이름의 기준은 `stages/lab.ts`다.** 거기 적힌 이름을 그대로 읽으면
  스테이지 파일을 고칠 일이 없다.
- 남은 목록은 코드가 안다: 콘솔에서 `__stages.backlog()`.
  현재 `platform`(3) → `conveyor`(4) → `wind`(5) → `thief`(6) →
  `lever`/`holdgate`(7) → `press`(8) 순.

### 12-4. 맵을 만들 때의 규칙 (전부 실측/코드로 확인된 것)

1. **코스 가운데(x=0)를 가로로 끊지 말 것.** 떨어진 공을 `main.ts`가 항상
   x=0으로 되돌리기 때문에, 그 자리가 허공이면 공이 무한히 다시 떨어진다.
   좌우로 가르는 낭떠러지는 얼마든지 파도 되고, 가로로 끊고 싶으면 아주
   좁아도 좋으니 x=0을 지나는 판을 남긴다.
   → `course.ts`가 맵을 지을 때마다 1m 간격으로 검사해서 `console.warn` 한다.
2. 골 앞 18m는 비운다.
3. `roller`는 자기 z에서 +Z로 18m 굴러간다. 그 구간에 다른 것을 두지 않는다.
4. `buttongate`의 발판은 문보다 3.6m 앞이다. 그 위에 낙하물을 떨어뜨리지 않는다.
5. 좁은 구간(다리/갈래길)에 낙하물·봇·피스톤·회전봉을 두지 않는다.
   `createHazards/createObstacles/createBots`가 **맵 전체에 하나의 반폭**만
   받으므로(지금은 `LANE_HALF`) 좁은 판 바깥 허공에서 헛돈다.
6. 협동 관문과 공 전용 틈은 지름길로 건너뛸 수 없는 자리에 둔다.

### 12-5. 같이 고친 것

- **B1 해결** — 튜토리얼 HUD가 한 번도 안 뜨던 버그. 조건이
  `world.map.id !== "goalrush"`였는데 그런 id를 가진 맵이 없었다.
  `MapDef.tutorial` 플래그를 보도록 바꿨다.
- `main.ts`에 하드코딩돼 있던 `7`(B6)을 `LANE_HALF`로 바꾸고, 왜 구간별 폭을
  줄 수 없는지 주석으로 남겼다.
- `npm test`에 `test:map`(46개)을 추가했다 — 스테이지가 전부 지어지는가,
  중앙선이 끊기지 않았는가, 골 앞 18m / roller 주행선이 비었는가,
  소품 id가 겹치지 않는가, 계획표와 등록 목록이 맞는가.
  **합계 245개** (기존 199 + 46).

---

## 13. 몸싸움 E / Q / F 배선 (2026-08-31)

> `scuffle.ts`의 로직은 이미 있었다. 이 절은 **그것을 게임에 붙인 기록**이다.
> `ragdoll.ts` / `control()` / 네트워크 구조 / 스테이지 1~3은 건드리지 않았다.

### 13-1. 무엇이 되었나

| 키 | 앞에 사람이 있으면 | 없으면 (예전 그대로) |
|----|-------------------|---------------------|
| E | 밀치기 — 비틀거리며 밀려난다. 안 넘어진다 | 공 줍기 / 놓기 |
| Q | 잡기 / 놓기 토글 — 끌고 다닌다 | 개인기 스톱턴 |
| F | 발차기 — 넘어뜨리고 날린다 | 공 킥 (차징 그대로) |

**기존 기능은 하나도 지우지 않았다.** 몸싸움이 실패하면(앞에 아무도 없거나,
쿨다운이거나, 상대가 무적이거나, 내가 누워 있으면) 예전 코드가 그대로 돈다.

### 13-2. 배선한 자리 (전부 `main.ts` fixedUpdate 의 host 분기)

```
scuffle.tick(dt)                  ← 판정보다 먼저. 쿨다운/무적/밀림 시간을 흘린다
allRags = 모든 캐릭터              ← 봇도 포함 (봇은 aim이 0이라 스스로는 못 때린다)
  ├ E: tryPush()  실패 → 기존 공 줍기          (control() 전, 원래 grabPending 자리)
  ├ Q: toggleGrab() 실패 → stopPending 을 남긴다 → 기존 스톱턴이 control() 뒤에서 처리
  ├ F: 공이 발밑이면 건너뛴다 → tryKick() 실패 → kickPending 을 남긴다 → 기존 공 킥
  ├ shoveDir()  → e.input 덮어쓰기  (대시/러시 뒤, control() 바로 앞)
  └ heldBy()    → e.input 에 끌리는 방향을 섞는다  (같은 자리)
updateHolds(dt)                   ← physics.step 전 (스텝 끝의 clearForces 전에 힘이 들어가야 한다)
```

정리는 기존 패턴을 따라갔다: `despawnPlayer`에 `scuffle.forget(rag)`,
`resetWorld()`에 `scuffle.reset()`.

### 13-3. F가 차징을 안 깨는 이유 (설계 판단)

F는 `hold-to-charge`다. **누르는 순간에는 아무 판정도 하지 않는다** — `kickPending`은
`releaseKickCharge()`가 키를 **뗄 때** 세우는 엣지이고, 몸싸움 판정은 그 엣지를
소비하는 자리에 붙었다. 그래서 탭/홀드를 따로 나눌 필요가 없었고 차징 감각도 그대로다.

그리고 **공이 발밑(`B.kickRange` = 1.85m)에 있으면 무조건 기존 공 킥**이다.
이 거리가 `ball.ts tryKick`이 성공하는 조건 그 자체라, "찰 수 있는 공이 있었는데
사람을 찼다"가 원리적으로 일어나지 않는다. 축구 상황은 전과 완전히 같다.

### 13-4. 잡기(Q)는 힘만으로는 성립하지 않았다 — 실측

`updateHolds()`의 당기는 힘만 있을 때, 잡은 사람이 뒤로 걸어가자
**상대가 따라온 거리는 0.87m뿐이었고 0.78초 만에 `GRAB_DISTANCE` 밖으로 밀려나
저절로 풀렸다.** 이 파일 머리말이 말한 그 문제(잡힌 사람의 `control()` 속도 서보가
힘을 매 스텝 지운다)가 잡기에도 똑같이 걸린 것이다.

그래서 밀치기와 같은 길을 썼다 — **이동 입력을 섞는다** (`SC.GRAB_DRAG = 0.7`).
통째로 뺏지 않는 이유는 잡힌 쪽이 반대로 걸어 저항할 수 있어야 하기 때문이다.
고친 뒤: 간격이 `GRAB_AHEAD`에 붙어 유지되고(실측 1.15~1.18m), 120스텝을 끌어도
안 놓치며, 브라우저에서 7.7m를 끌고 다녔다.

### 13-5. 기존 bump 와의 충돌 처리

`updatePlayerBumps`(사람끼리 부딪히면 같이 자빠지는 장치)는 **지우지 않았다.**
두 경우만 건너뛴다.

1. 방금 E/F로 맞은 사람 (`scuffle.isImmune`) — 날아가는 속도 그대로 제3자에게
   꽂히면 bump가 다시 양쪽을 넘어뜨려서 한 번의 발차기가 연쇄 경직이 된다.
2. 잡고 끄는 중인 둘 — 붙어 있는 게 정상인데 접근속도가 잠깐만 넘어도 둘 다 자빠진다.

`isImmune()`은 이번에 `scuffle.ts`에 더한 **읽기 전용 접근자**다 (로직 변경 없음).

### 13-6. 검증

- `npx tsc --noEmit` 통과 / `npm run build` 통과 (모듈 37개 — 예전엔 36개였다.
  `scuffle.ts`가 아무 데서도 import되지 않아 번들에 아예 없었다는 뜻이다)
- `npm test` **350개 통과** (기존 300 + `test:scuffle` 50). 기존 테스트는 손대지 않았다.
- `test/scuffle-test.ts` — 14개 묶음. "호출이 null이 아니다"가 아니라 **밀린 거리 /
  끌려온 거리 / 날아간 거리**를 잰다. 폴백은 "무엇이 눌렸나"가 아니라 **무엇이
  실행됐나**(tag)로 확인한다. 600스텝 난타 안정성, 드리블 보존도 같이 본다.
- **실제 브라우저 2개 29개 통과** (`test/.browser2.mjs`는 일회용이라 지웠다).
  host/비-host 양쪽에서 E/Q/F가 나가고, 양쪽 화면의 위치가 0.5m 안에서 일치했다.

### 13-7. 브라우저 2인 검증을 다시 하려면 (다음 사람을 위한 메모)

puppeteer가 없으므로 CDP를 `ws`로 직접 쳤다. 헤매기 쉬운 곳이 네 군데다.

1. **창을 따로 띄워야 한다.** 한 창의 비활성 탭은 `requestAnimationFrame`이
   **0회/1.5초**라 게임 루프가 아예 선다. `Target.createTarget({newWindow:true})`.
   헤드리스는 활성 창도 3fps 수준이라 물리 검증이 안 된다 — 창을 띄우고 해야 한다.
2. **「서로조종」을 기억할 것.** A탭의 키는 A의 캐릭터가 아니라
   `__dbg.controlled()`가 가리키는 캐릭터를 움직인다. 위치도 그 id로 읽어야 한다.
3. **비-host에서 `__dbg.teleport()`는 소용없다.** 다음 스냅샷이 덮어쓴다.
   배치는 전부 host 탭에서 하고, 상대는 그 자리에 둔 채 내가 옆으로 간다.
4. **`__dbg.pressKick()`은 차징 세기가 0이다** (공 속도 0.18 m/s). 실제 킥을 보려면
   `Input.dispatchKeyEvent`로 KeyF를 0.9초 눌렀다 떼야 한다.
   이번에 `__dbg.pressQ()`와 `__dbg.scuffle()`(잡기/밀림/무적 현황)을 더해 두었다.

### 13-8. 남은 것

- **밸런스는 숫자로만 맞췄다.** 손맛(미는 세기 / 끌리는 무게감 / 날아가는 거리)은
  사람이 직접 만져 봐야 한다. 조절은 전부 `scuffle.ts`의 `SC` 한 곳에서 한다.
- 봇은 몸싸움을 **당하기만** 한다 (조준을 안 주므로 스스로는 못 때린다).
  봇에게 시키려면 `bot.ts`가 aim을 채우고 tryPush/tryKick을 부르면 된다.
- 연출은 기존 배관(`fx.dash/kick`, `netSfx("hit")`, `showMove`)을 그대로 썼다.
  전용 소리·이펙트는 없다.
- 잡은 채로 골에 들어가면 어떻게 되는지는 안 봤다.

---

## 14. 몸싸움 손맛 튜닝 (2026-08-31)

> 13절에서 붙인 E/Q/F를 **실제 플레이 기준으로 조절한 기록.**
> 구조는 그대로다 — `ragdoll.ts` / `control()` / 네트워크 / 스냅샷 / 공 물리 /
> 스테이지 1~3 전부 안 건드렸다. 바뀐 것은 `scuffle.ts`의 상수와 충격량 거는
> 방식, 그리고 `main.ts`의 연출·잡기 저항뿐이다.

### 14-1. 튜닝 전에 잰 기준선 (문제가 여기서 다 나왔다)

| 잰 것 | 튜닝 전 | 무엇이 문제였나 |
|-------|---------|-----------------|
| E 거리별 밀린 거리 | 0.7m든 1.95m든 **전부 1.46~1.48m** | 어디서 밀어도 결과가 같다 = "제대로 맞혔다"가 없다 |
| E 각도별 | 0도든 63도든 **전부 1.48m** | 스치듯 맞아도 정통과 똑같다 |
| F 기울기 최소 | **0.27** | 안 뒤집힌다. 옆으로 기운 채 미끄러질 뿐 |
| Q 저항 | 버텨도 16.61m 끌림 (가만히 16.63m) | **저항이 아무 의미가 없다** = 조작을 통째로 빼앗긴다 |
| E→F 연계 | 안 들어감 | 한 번 밀면 상대가 사거리 밖으로 나가 버린다 |

### 14-2. 바꾼 값

```
E  PUSH_FORCE   55 -> 46     PUSH_UP  9 -> 7    PUSH_COOLDOWN 0.5 -> 0.42
   PUSH_STUN    0.3 -> 0.32  (세기에 비례해서 0.35~1.0배로 쓴다)
   PUSH_MIN     0.4          (신규) 스치면 남는 세기 비율
   PUSH_TWIST   20 / PUSH_TWIST_AT 0.3  (신규) 상체를 가슴 위쪽에서 젖힌다
F  KICK_UP      30 -> 34     KICK_KNOCKDOWN 1.0 -> 1.15
   KICK_SPIN    11           (신규) 진행 방향으로 앞구르기
Q  GRAB_RESIST  0.8 rad      (신규) 버틸 때 끌리는 방향을 트는 각
```

### 14-3. 튜닝 후 (같은 방법으로 다시 잼)

| | 결과 |
|---|---|
| E 거리별 | 1.36m(코앞) → 0.97m(사거리 끝), 상체 회전 5.2 → 2.2 |
| E 각도별 | 1.22m(정면) → 0.80m(경계 63도) |
| F | 평지 5.50m / 기울기 **-0.99**(완전히 뒤집혀 구른다) / 회전 15.9 rad/s |
| Q | 버티면 1.5초 뒤 뿌리침 · 안 버티면 4초 내내 17m 끌림 |
| E→F | 밀고 0.75초 뒤 쫓아가서 들어감 |
| 연속 E | 10초에 3~4회, 조작 뺏긴 시간 0.85초, **넉다운 0회** |

### 14-4. 알아낸 것 세 가지 (다음에 값을 만질 사람에게)

1. **`control()`이 이동 입력의 크기를 무시한다.** 정규화해서 방향만 쓴다.
   그래서 "약하게 밀린다"를 입력 세기로 표현할 수 없다. E의 세기는 결국
   **밀림이 지속되는 시간**(PUSH_STUN)으로만 조절된다.
2. **잡기 저항을 방향 섞기로 만들면 이분법이 된다.** 위와 같은 이유로, 섞은
   결과가 어느 쪽이든 전속력이 되어 0.28을 경계로 "전속력으로 끌려감"과
   "전속력으로 도망감"이 그냥 뒤집혔다(0.24 = 16.6m 끌림, 0.34 = 0.37초 만에
   풀림). 그래서 **방향을 옆으로 트는** 방식으로 바꿨다. 속도는 그대로인데
   잡은 사람 쪽 성분만 cos만큼 줄어서, 버티는 동안 옆으로 미끄러지며 끌려간다.
3. **더 세게 찬다고 더 날아가지 않는다.** KICK_FORCE를 135 → 165로 올렸더니
   평지 이동이 5.50m → **4.79m로 줄고** 뒤집힘도 -0.99 → -0.55로 약해졌다.
   앞으로 세게 밀수록 몸이 땅에 처박혀 마찰로 에너지를 잃는다. 거리를 정하는
   건 `KICK_UP` 쪽이다 (135 기준: 26→3.36m / 30→3.71m / 34→5.50m).

### 14-5. 붙인 연출 (전부 기존 배관 재사용, 새 오디오·새 FX 없음)

- **세기가 연출에 실린다.** `ScuffleHit.power`(0..1)가 `netSfx("hit")`의 볼륨·피치,
  `fx.touch` 크기, `addShake` 세기를 함께 움직인다. 툭 스친 것과 정통으로 박은
  것이 귀와 화면으로 구별된다.
- **대사.** 기존 `dramaLine` 배관에 `PUSH_LINES` / `PUSH_HARD_LINES`(0.8 초과) /
  `KICK_LINES` / `GRAB_LINES` / `STEAL_LINES`를 얹었다. 겹쳐 뜨지 않는 간격은
  기존 장치가 이미 처리한다.
- **잡힌 쪽에도 알려준다.** 안 그러면 몸이 갑자기 끌려가는 이유를 모른다.
  (`붙잡혔다! 반대로 걸으면 뿌리칠 수 있다`)
- **공을 놓친다 (`dropCarried`).** 세게 맞은 E(power > 0.72)와 모든 F는 상대가
  안고 있던 공을 떨어뜨린다. 장애물에 맞았을 때의 기존 처리와 같은 일을 한다.
  **친구가 공을 안고 골로 달릴 때 막을 수단**이 이걸로 생겼다.

### 14-6. 검증

- `npx tsc --noEmit` / `npm run build` 통과 (모듈 37개)
- `npm test` **367개 통과** (기존 300 + scuffle 67). 기존 테스트는 손대지 않았다.
  `test:scuffle`에 TEST 15~18을 더했다 — 세기 기울기, 뒤집힘, 잡기 저항, 연타.
- **실제 브라우저 2개 27개 통과** (2회 연속 동일). E 10회 평균 0.78m·넉다운 0,
  F 10회 넉다운 8/8·회전 7.2 rad/s, Q 3초에 12.4m 끌기, 공 든 사람 밀치면 놓침,
  공 킥 8.87 m/s 그대로, 양쪽 화면 차이 0.00m.

### 14-7. 브라우저로 손맛을 잴 때의 함정 (13-7에 이어서)

1. **라운드 제한시간(190초)에 걸린다.** 여러 구간을 이어서 재면 도중에 결과
   화면으로 넘어가고, 그 뒤 측정값이 전부 0이 된다. `__dbg.net.send({type:'restart'})`
   로 다시 시작하면서 재야 한다.
2. **공을 치울 수가 없다.** 뒤로 차면 백스톱이 도로 끌어오고, 앞으로 차면
   떨어져서 근처에 되살아나고, 그냥 두면 드리블이 발밑에 붙여 놓는다(무엇을
   해도 1.2~1.7m로 수렴). **안고 걸어가서 내려놓아야** 그 자리에 남는다.
3. **X 방향으로 옮기면 낙사한다.** 코스는 Z로 뻗은 통로다. 배치는 Z축 위에서 한다.
4. **회전의 봉우리를 못 잡는다.** CDP 왕복이 30~50ms라 0.1초짜리 피크를 놓친다
   (매 스텝 재면 15.9인데 200ms 간격으로는 4.8이 나왔다). 크기를 재려면
   헤드리스로, 브라우저에서는 "도는가 아닌가"만 본다.
5. **넉다운을 봤다고 그게 E 탓이 아니다.** 코스의 낙하물·장애물이 가만히 선
   사람을 때리고, 쫓아가 부딪히면 기존 `updatePlayerBumps`가 양쪽을 넘어뜨린다.
   실측으로 같은 구간이 실행마다 0회 / 3회 / 7회로 흔들렸다. **E를 안 누르는
   대조군**을 같이 돌려야 출처를 가릴 수 있다 (대조군도 같으면 코스 탓이다).

### 14-8. 아직 부족한 것

- **연출이 소리 하나로 다 돈다.** E도 F도 `"hit"` 한 종류를 볼륨·피치만 바꿔
  쓴다. 새 오디오를 안 넣는 조건이라 여기까지다. 전용 타격음이 생기면 체감이
  한 번 더 올라간다.
- **F가 실제 코스에서는 0.8m밖에 안 밀린다** (평지는 5.5m). 난간·좁은 판·마찰
  때문이다. 넘어뜨리고 굴리는 그림은 확실하지만 "뻥 날아간다"는 넓은 자리에서만
  보인다. 좁은 구간에서도 시원하려면 지형 쪽을 봐야 한다.
- **봇은 여전히 당하기만 한다.** 봇이 밀치거나 잡으면 훨씬 시끄러워질 것이다.
- 셋 이상이 한꺼번에 엉킬 때의 그림은 헤드리스 2인 난타로만 봤다.


---

## 15. 놀리는 맛 — 방향 판정 · 되튕김 · 후려치기 · 몸짓 (2026-08-31)

14절이 만든 E/Q/F는 "된다"까지였다. 이 절은 그 위에 **상황마다 다른 그림**을
얹은 것이다. 새 시스템도 새 오디오도 새 네트워크 메시지도 없다 — 전부 기존
충격량/입력덮어쓰기/스냅샷/`"hit"` 소리를 나눠 쓴다.

```
npx tsc --noEmit   → exit 0
npm test           → 433 passed, 0 failed
                     (net 29 / room 28 / ragdoll 39 / swap 11 / explosion 7 /
                      game 44 / ball 41 / map 46 / gimmick 55 / scuffle 107 / flair 26)
npm run build      → exit 0 (dist/assets/index-DPROZY3b.js)
```

### 15-1. E는 이제 **어디를 맞혔는지**를 본다 (`scuffle.ts`)

판정 기준은 **맞은 사람의 몸통이 보고 있던 쪽**이다 (`facingOf()`). 이동 입력이
아니라 자세를 쓰는 이유는, 가만히 선 사람에게도 등이 있기 때문이다.

| 맞은 쪽 | 무엇이 달라지는가 | 실측 (같은 자리·같은 세기) |
|---------|-------------------|---------------------------|
| 등 (`back`) | 무게중심이 앞으로 쏟아진다. 제일 크게 밀린다 | 앞으로 **1.45m** |
| 정면 (`front`) | 덜 밀리고 상체가 크게 젖혀진다 (휘청) | 뒤로 1.08m, 초기 젖힘 0.23 (등은 0.11) |
| 옆 (`side`) | 몸이 팽그르르 돌고 비스듬히 미끄러진다 | 옆으로 **0.69m**, 회전 **1.02 rad** |

- 젖히는 몫은 **정면에 몰아주고 등에서 뺐다**. 둘 다 크게 젖히게 두면 정면만
  넘어졌다 (뒤로 밀리면서 뒤로 젖혀지므로 그대로 넘어간다 — 기울기 0.99).
  E는 넘어뜨리는 기술이 아니다. 그건 F의 몫이다.
- **공을 떨어뜨리는 문턱이 방향마다 다르다** (`main.ts`): 등 0.5 / 옆 0.62 /
  정면 0.78. 그래서 "공 든 친구 뒤로 돌아가기"가 실제로 이득인 행동이 된다.

### 15-2. F로 찬 사람이 벽에 박으면 되튕긴다

```
차인다 → 감시 시작(1.2초) → 0.1초 뒤 무장 → [속도가 확 죽었다] AND [벽에 닿아 있다] → 되튕김
```

두 조건을 **같이** 본다. 어느 하나만으로는 안 됐다:

- 속도만 보면 — 땅에 처박히는 순간에도 한 스텝에 2.97~4.04 m/s가 사라진다
  (마찰만으로는 0.17 m/s다). 평지에서 아무 데나 튕겼다.
- 접촉만 보면 — 벽을 스치기만 해도 닿아 있다.

벽/바닥은 **접촉 법선**으로 가른다 (`REBOUND_WALL_DOT`). 충돌 이벤트를 안 쓰고
`physics.contacts`를 읽는 이유는, 래그돌 바디가 15개라 `collide` 리스너를 달면
자기 팔다리까지 전부 걸리기 때문이다.

`REBOUND_ARM`(0.1초)이 필요한 이유: 발차기 충격량은 골반 **하나**에 걸려서 찬
순간 골반만 16.4 m/s가 되고, 다음 스텝에 관절이 나머지 12kg을 끌면서 10.1로
떨어진다 — 한 스텝에 6.3 m/s 손실이다. 벽보다 큰 값이라 감시 전에 잦아들려야 한다.

실측: 공중에서 벽을 박으면 진행 방향이 뒤집히고(vz +6.7 → **-3.3**) 0.8m 물러난다.
바닥에 붙어서 박으면 접촉 솔버가 되미는 힘을 그 자리에서 먹어 0.2m 정도다.

### 15-3. Q — 후려치기와 줄다리기

- **후려치기**: 잡은 채로 상대 방향이 도는 각속도가 `WHIP_RATE`(2.2 rad/s)를
  넘으면 접선 방향으로 던진다. `WHIP_DOWN_RATE`(4.6)를 넘으면 손에서 놓치고
  상대는 넘어진다. 문턱은 실측으로 골랐다 — 곧게 끌면 0.4, 상대가 버티며
  발버둥쳐도 1.2, 90도를 홱 트는 동작이 2.7이다.
- **줄다리기**: 서로 잡으면 당기는 힘이 양쪽 다 `GRAB_TUG`(0.5)로 준다. 안 줄이면
  두 힘이 정면으로 부딪혀 둘 다 부들부들 떨다 물리가 튄다. 실측으로 3초 동안
  둘 다 0.2m 안에서 팽팽하게 버텼다.
- 줄다리기 중에는 후려치기를 **끈다**. 서로 잡고 버티면 상대 방향이 저절로
  4.96 rad/s로 흔들려서, 아무도 돌리지 않았는데 사람이 날아갔다.

### 15-4. 달리다 부딪히면 넘어지지 않고 휘청인다

기존 `updatePlayerBumps`는 접근속도 3.2 m/s를 넘으면 **둘 다 넘어뜨린다**.
그 아래는 아무 일도 없었다 — 마주 걸어와 부딪히면 서로를 통과한 것처럼 보였다.
그 사이(`BUMP_SOFT_CLOSING` 1.5 ~ 3.2)에 한 칸을 넣었다. 기존 넘어뜨리기 코드는
한 줄도 안 바뀐다.

### 15-5. 몸짓 — `client/src/flair.ts` (새 파일, 266줄)

달릴 때 뒤로 젖히고, 급정지에 앞으로 쏠리고, 방향을 홱 틀면 좌우로 흔들리고,
세게 떨어지면 털썩 주저앉는다. 애니메이션도 새 상태기계도 없다 — `control()`
**뒤에서 상체 토크만 얹는다**.

**세기를 눈으로 안 고른다.** `control()`의 몸 세우는 토크는
`P.uprightTorque(70) × sin(기울기)`라, T N·m를 걸면 `sin(θ) = T/70`에서 멈춘다.
넘어짐 판정은 `sin(θ) > 0.907`이므로, 제일 센 몸짓(30 N·m → 0.43)도 절반이다.
= **이 파일은 넘어뜨릴 수 없다.** 테스트가 이 식을 그대로 검사한다.

측정값 (몸짓 켬 / 끔):

| | 켬 | 끔 |
|---|---|---|
| 달릴 때 상체 (음수 = 뒤로 젖힘) | **-0.52** | -0.10 |
| 급정지 흔들림 폭 | **0.92** | 0.55 |
| 방향 전환 좌우 기울기 | **0.32** | 0.19 |
| 3초 이동 거리 | 13.38m | 13.38m |
| 최고 속도 / 전속력까지 / 정지까지 | 4.75 · 19스텝 · 24스텝 | 4.74 · 19스텝 · 23스텝 |

마지막 세 줄이 이번 작업의 금지선이었다 — **조작 반응성은 한 프레임도 안 바뀐다.**
몸짓은 이동 입력에 손대지 않고 상체 각도만 만들기 때문이다.

### 15-6. 고친 잠재 버그 — 연출 충격량이 사람을 넘어뜨리고 있었다

밀 때 손에 `armThrust: 16` N·s를 그대로 걸고 있었다. 손은 **0.3kg**이라 그 한
방에 **53 m/s**가 된다. 그 손이 다음 스텝에 상대 골반을 스치면 `ragdoll.ts`의
충격 감지(`P.impactSpeed` 13 m/s, **접촉점** 속도로 잰다)에 걸려서 **미는
시늉만으로 상대가 넘어졌다** (실측 rel=14.1 m/s). `grabReach`(43 m/s),
`footThrust`(37 m/s)도 같은 상태였다.

그래서 팔다리 연출값을 전부 **속도(m/s)** 로 바꾸고 `flick()`이 질량을 곱하게
했다. 팔이 통째로 같은 속도로 나가므로 손만 총알처럼 튀지 않고, 그림으로도
"팔을 뻗었다"가 된다. 몸통(5kg)은 원래 문제가 없어서 충격량 그대로다.

같은 이유로 F의 팔 허우적은 **뒤가 아니라 옆으로** 벌린다. 뒤로 밀면 어깨가
몸 중심보다 위라 그 충격량이 앞구르기와 **정반대 토크**가 된다 — 실측으로
뒤집힘이 -0.23에서 0.03으로 죽어서, `KICK_SPIN`을 팔이 도로 지우고 있었다.
옆으로 벌리니 -0.88로 돌아왔다.

### 15-7. 상황별 연출 (새 오디오 없이)

`"hit"` 하나를 볼륨·피치로 나눠 쓴다. 등 1.18 / 정면 1.42 / 옆 1.56 / 되튕김
0.62 / 가벼운 충돌 1.7. 화면 흔들림과 대사도 상황마다 다르다 —
`PUSH_SOFT/BACK/SIDE/HARD`, `REBOUND`, `WHIP`, `TUG`, `NUDGE` 목록이
기존 `dramaLine` 배관을 그대로 쓴다 (겹쳐 뜨지 않게 간격이 걸린다).

### 15-8. 아직 부족한 것

- **되튕김이 바닥에 붙어서 박으면 밋밋하다.** 공중에서 기둥을 박으면 0.8m
  튕겨 나오는데, 이미 구르는 중이면 0.2m다. 접촉 솔버가 되미는 힘을 그 자리에서
  먹는다. 세기를 올려도 이득이 빠르게 줄어든다 (0.9→0.17m, 2.0→0.45m).
- **봇은 여전히 당하기만 한다.** 봇이 밀치거나 잡으면 훨씬 시끄러워질 것이다.
- **정면 E의 "휘청"이 자세로만 보이고 소리로는 안 들린다.** 전용 타격음이
  생기기 전까지는 피치 차이가 전부다.
- **줄다리기의 승패가 없다.** 지금은 팽팽하게 버티다 한쪽이 Q를 놓으면 끝이다.
  버틴 시간이 쌓이면 한쪽이 나가떨어지는 식이 붙으면 더 시끄러울 것이다.
- 셋 이상이 한꺼번에 엉킬 때의 그림은 여전히 헤드리스 2인 난타로만 봤다.

---

## 16. feature/ui 통합 + 봇 + 드리블 튜닝 (2026-08-31)

> feature/logic(게임플레이 기준)에 feature/ui의 가치 있는 기능만 골라 얹었다.
> **ragdoll.ts / control() / 네트워크 권위 구조 / scuffle / stages·maps 구조는
> 하나도 안 건드렸다.** 검증: `tsc 0` / `npm test 445 passed 0 failed` /
> `npm run build 0` / 헤드리스 브라우저(단일 3스테이지 완주 + 2인 동기화 + E/Q/F).

### 16-1. feature/ui 에서 가져온 것

| 영역 | 방식 |
|------|------|
| 오디오 파일 + BGM(스테이지별 3곡 + 메뉴 1곡) + 볼륨 분리 | `audio.ts` 교체(logic은 base와 동일했음) + `client/public/audio/` 14개 + `main.ts` 배선(loadSampleUrls / playBgm / updateBgmForPhase / 카운트다운 GO 에서 맵 BGM 시작) |
| 설정 UI (⚙ 오버레이, BGM/SFX 슬라이더, localStorage 저장·복원) | 신규 `settings-ui.ts` + `index.html` 오버레이/CSS + `main.ts initSettingsUI(sfx)` |
| 공 충돌음 / 관중 환호 / 강한 킥·래그돌 진입 사운드 | `main.ts` onBallCollide 리스너 + `netSfx` 로 friend 화면에도 |
| 공유 링크 (`#CODE` URL 자동 참가, 클릭 복사) | `menu.ts` 교체 + 신규 `ws-url.ts` |
| vite ws 프록시 / 터널·공유 스크립트 / `test:ws`(12개) | `vite.config.ts` 교체, `scripts/`, `test/ws-proxy-test.js` |

**버린 것**: feature/ui 의 world.ts·obstacles.ts 기믹 구현(platform/conveyor/wind/
ballsocket + bumper) — logic 이 같은 기믹을 더 완성도 높게 이미 구현했다. maps.ts
수정도 버림(logic 이 `maps/` 폴더로 분해). feature/ui 의 `ball.ts` 드리블 상수
변경도 버림 — `test:ball` 의 개인기 테스트 2개를 깨뜨린다(그 브랜치에서 실측됨).

### 16-2. 방해꾼 AI 봇 (bot.ts)

feature/ui 의 역할 시스템을 그대로 가져왔다: bot id 로 역할이 정해진다
(`roleForBot`) — chaser(공 걷어냄 + 가끔 태클해 둘 다 넘어짐) / blocker(공과 골
사이 길목) / bruiser(사람만 밀어냄). 봇끼리는 안 때린다(대상이 `humans` 뿐).
`main.ts` 는 `bots.update()` 호출부만 ctx 객체로 바꾸고 `r.tackled` 를 처리한다.
넉백은 기존 봇 공-걷어내기 / 장애물 넉백과 **같은 패턴**(raw impulse +
`rag.knockdown()` 공개 API) — scuffle 경유는 봇에 가짜 입력 주입이 필요해 뺐다.

**스테이지 배치**: 스테이지 2(양쪽 레버)에만 `botSpawns: [[3.4, -26]]` 하나.
- 스테이지 1 은 몸풀기라 무봇.
- 스테이지 3 은 갈래길·낭떠러지·외줄 다리·회전봉·구르는 공·피스톤 둘로 이미
  최고 난도다. 봇 2기를 넣었더니 오토파일럿 완주가 실패했다(z-78 부근 정체).
  그래서 뺐다. 봇은 넓고 장애물이 성긴 스테이지 2 담당이다.

### 16-3. 드리블 튜닝 — `B.turnBite` 0.45 → 0.6

유일하게 바꾼 공 상수다. 90도로 홱 꺾을 때 공이 옛 방향 운동량의 55%를 안고
발 범위(2.6m) 밖으로 달아나던 것(계측 1.58m → 3.75m = 놓침)을, 방향 전환 터치
한 방으로 2.3~2.4m 에 남겨 드리블이 계속 붙게 한다. 여전히 40%는 흘러 나가므로
**천천히 도는 회전·지그재그에서는 공을 놓친다** = 급회전의 위험은 그대로 남는다.
직진 터치 경로와 킥·개인기는 이 값을 안 쓴다. `test:ball` 41개 전부 통과.

> 나머지 `B` 상수는 안 건드렸다(7-5: 소수점 최적화 금지). "정지한 공의 킥이
> 약하다"(B5, 8.9 vs 13.5 m/s), "완만한 회전에서 공이 샌다"는 미해결 — 사람이
> 브라우저에서 손맛으로 잡아야 하는 자리다.
